#set document(title: "Belief Geometry on the Random Hierarchy Model")
#set page(margin: 2.5cm)
#set text(size: 11pt)

#align(center)[
  #text(size: 18pt, weight: "bold")[Belief Geometry on the Random Hierarchy Model]

  A small transformer linearly encodes the exact tree posterior

  #datetime.today().display("[month repr:long] [day], [year]")
]

= Introduction
